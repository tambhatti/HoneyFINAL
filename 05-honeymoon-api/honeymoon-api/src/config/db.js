'use strict';
/**
 * DEPRECATED — This in-memory database was used in early development sessions.
 * All controllers now use Prisma (src/config/prisma.js).
 * This file is kept temporarily to prevent import errors during migration.
 * TODO: Delete this file once confirmed no remaining imports.
 */
throw new Error(
  '[db.js] The in-memory database has been removed. ' +
  'Import prisma from ../config/prisma instead.'
);
